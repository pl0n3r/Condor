framework:
  rate_limiter:
    platform_staff_invite_resend:
      policy: fixed_window
      limit: 5
      interval: '1 hour'

    invitation_activation:
      policy: sliding_window
      limit: 10
      interval: '15 minutes'

    storefront_checkout:
      policy: sliding_window
      limit: 10
      interval: '5 minutes'

    control_bot_api:
      policy: sliding_window
      limit: 60
      interval: '1 minute'
