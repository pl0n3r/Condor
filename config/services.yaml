parameters:

services:
  _defaults:
    autowire: true
    autoconfigure: true

  App\:
    resource: '../src/'
    exclude:
      - '../src/Kernel.php'

  App\Shared\Version\AppVersion:
    arguments:
      $projectDir: '%kernel.project_dir%'
