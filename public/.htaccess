DirectoryIndex index.php

<IfModule mod_rewrite.c>
    RewriteEngine On

    # Los archivos públicos reales se sirven directamente.
    RewriteCond %{REQUEST_FILENAME} !-f

    # El resto entra al front controller de Symfony.
    RewriteRule ^ index.php [QSA,L]
</IfModule>
